#include <list>
#include <vector>
#include <algorithm>
#include <concepts>

/* given
 *  
 * */
template<class N>
std::vector<N> intersections(std::function<N(N)> f, std::function<N(N)>, std::pair<N, N> range) {

}

int main() {

}
